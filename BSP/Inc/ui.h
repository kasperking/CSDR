/* ui.h – convenience alias for CSDR UI layer
 * st7789.h contains ALL declarations (driver + UI).
 * ui.h just includes st7789.h so code that used to include
 * only ui.h still works.
 */
#ifndef __UI_H
#define __UI_H
#include "st7789.h"
#endif /* __UI_H */
